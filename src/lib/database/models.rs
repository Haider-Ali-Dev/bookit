#[derive(Debug, Clone)]
pub struct Password {
    pub password: String
}

#[derive(Debug, Clone)]

pub struct Id {
    pub id: i32
}


#[derive(Debug, Clone)]
pub struct Email {
    pub email: String,
    pub bookmark_name: String
}
